/**
 * Default number of rows per page for standard list/data-table views.
 * Keep list cards consistent by passing this to the DataTable `pageSize` prop
 * rather than hardcoding a per-view value.
 */
export const LIST_PAGE_SIZE = 10;
